version: "3.8"
services: 
  app:
    build: 
      context : .
    expose:
      - "9999"
  nginx: 
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    ports:
      - "9000:80"